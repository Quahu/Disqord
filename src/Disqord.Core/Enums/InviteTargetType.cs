namespace Disqord
{
    /// <summary>
    ///     Represents the target type of an invite to a voice channel.
    /// </summary>
    public enum InviteTargetType : byte
    {
        Stream = 1,
        EmbeddedApplication = 2
    }
}
